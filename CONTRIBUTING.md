# 协作与交付指南

本仓库采用“按问题提交材料、论文负责人统一整合”的方式协作。建模和绘图同学提交可复现材料，论文负责人维护 `main.tex`、`contents/`、`config/` 和最终文稿。

## 文件放在哪里

以问题一为例：

- 方法、公式、结果解释和待确认事项：`docs/handoffs/q1/README.md`
- 完整可运行代码：`code/q1/`
- 清洗数据、模型输出和统计结果：`results/q1/`
- 绘图脚本：`scripts/plotting/q1/`
- 可编辑图源：`figures/source/q1/`
- 准备用于论文的最终图片：`figures/final/q1/`
- CSV、XLSX 或 LaTeX 表格：`tables/q1/`

问题二至问题四分别使用 `q2`、`q3`、`q4`。原始赛题和附件位于 `data/raw/problems/`，不得覆盖或修改；代码产生的文件写入 `results/`。

## 每次交付必须包含

1. 更新对应问题的 `docs/handoffs/qN/README.md`。
2. 写明运行环境、入口命令、输入和输出路径。
3. 每个关键数值注明对应的结果文件、工作表或字段。
4. 每张图写明数据来源、横纵轴、单位和支持的结论。
5. 涉及随机过程时固定并记录随机种子。
6. 记录本次实际使用的 AI 工具、用途和人工核验方式。

不要只提交截图、无法编辑的公式图片或零散代码片段。表格优先使用 CSV；需要多个工作表时使用 XLSX。论文图优先使用 PDF，PNG 建议至少 300 DPI，同时提交生成该图的脚本。

## GitHub 提交流程

1. 开始工作前同步 `main`。
2. 每项独立任务新建一个分支，例如 `q1/drying-model` 或 `q3/sensitivity-plot`。
3. 只修改自己负责的问题目录；如需修改论文正文或公共配置，先在合并申请中说明。
4. 提交信息写清题号和内容，例如 `q1: add baseline model and results`。
5. 推送分支并创建 Pull Request，填写仓库提供的检查清单。
6. 根据审阅意见继续向同一分支提交；由论文负责人最终合并。

一个 Pull Request 应对应一项可以独立检查的交付。不要直接向 `main` 推送未核验的结果。

## 提交前自查

- 代码从仓库根目录运行，不依赖个人电脑的绝对路径。
- 密码、令牌、个人身份信息和无关文件没有进入提交。
- 原始数据未被覆盖。
- 结果可以由已提交的代码和输入复现。
- 文件名含义清楚；同一结果不要用 `final2`、`最新版` 等名称反复复制。
- 修改论文后运行 `latexmk main.tex`，并检查 `build/main.pdf`。

## 本地环境

在仓库根目录创建虚拟环境后执行 `python -m pip install -r requirements.txt`。所有入口均使用仓库相对路径；绘图脚本先运行对应问题的主程序生成 `results/` 数据，再写入 `figures/final/`。
