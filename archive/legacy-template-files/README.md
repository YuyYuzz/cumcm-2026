# 旧模板散文件归档

本目录保存项目切换到 Sustainable-Enjoyment 模板前遗留在仓库根目录和
`figures/` 下的示例文件。它们仅用于追溯，不参与正式论文或版式样张编译。

- `example.tex` 与 `example.pdf`：旧模板示例；
- `cumcm2026.sty`：已停用的旧样式，会覆盖当前模板排版；
- `example-1.png` 与 `figures/`：旧示例配套图片。

正式电子版唯一入口为仓库根目录的 `main.tex`。不要从本目录复制样式或
身份信息字段到参赛论文及支撑材料中。
